# Stores 边界说明

`src/stores` 只用于纯客户端本地状态，不作为接口缓存层。

允许放入：

- 导航、抽屉、弹窗显隐
- 本地偏好设置
- 仅前端存在的筛选条件
- 登录态概要这类轻量本地会话信息

禁止放入：

- 店铺详情
- 笔记详情
- 搜索结果
- 用户主页远程数据
- 任何本应由 TanStack Query 管理的服务端数据

当前状态：

- 本项目目前没有明确需要长期驻留的全局本地状态
- 因此本目录先保留边界说明，不为了“目录完整”而强行创建 Zustand store
